# A3 / F4 现场诊断（优化版）
更新时间：2026-04-01

## 固件
- `E:\相位\program\green\output\GreenLaser_Unified.hex`（已刷入）

## 结论先看
- 本轮 `F4` 选点：`work_apd=848`
- `A3` 在 `APD=848` 时外光路峰值约 `2057~2058`，按 `>2100` 判据属于“无有效信号”
- 外路并非完全死：当 APD 提到 `920` 时可上到 `2255~2315`；到 `960` 已接近/进入饱和（`4095`）

## 关键数据
- `F4` 回包：`AA FE F4 00 03 50 00 A7 BB FF`
- `F4` 日志要点：
  - `vbr=992`
  - `best=848`
  - `pick_ax=12550`

- `A3`（默认 APD）：
  - `inner_max=2298`
  - `outer_max=2057`

- `A3`（强制 APD=848）：
  - `inner_max=2304`
  - `outer_max=2058`

## A3 扫 APD（LL, freq=3, 每点3次）
- `760`: `[2056, 2054, 2047]`（median `2054`）
- `800`: `[2055, 2057, 2058]`（median `2057`）
- `840`: `[2056, 2057, 2056]`（median `2056`）
- `880`: `[2060, 2064, 2061]`（median `2061`）
- `920`: `[2160, 2315, 2255]`（median `2255`）
- `960`: `[4095, 4095, 4091]`（median `4095`）
- `1000`: `[4093, 2066, 4086]`（median `4086`）

## 解释
- 现在的 `F4=848` 处于外路“刚在基线附近”的区间，未跨过你们 `2100` 线。
- 外路有效信号区间更靠高 APD（约 `920`），但再往上容易进入饱和不稳定区。
