# Repo Push Queue - 2026-04-29

## 流程

1. 先看 `★ 待确认提交`。
2. 对每个仓库确认“这次改了什么”。
3. 标记为 `确认提交` / `继续开发` / `先 pull`。
4. 再逐仓执行 `git commit` 和 `git push`。

- 待确认提交仓库数: `6`
- 需先 pull 仓库数: `0`

## ★ 待确认提交

### ★ bootloader
- 改了什么：切到 UART 启动链路，并改为读取 APP `firmware_meta_t` 做 size/CRC 校验；同时混入构建产物。
- 目的：让 bootloader 在跳转 APP 前按元数据做完整性校验。
- 当前建议：`评审后补提交并推送`
- 下一步：评审后决定是否提交推送。

### ★ J2N4C-150L
- 改了什么：重排背光/电池脚位，改按键与 LCD 流程，并引入单发/双发 APD 快速采样链路；同时混入 `project/output/*` 与 `JLinkLog.txt`。
- 目的：把 150L 工程切到新的硬件映射和测量链路，为单双发测距口径统一做准备。
- 当前建议：`评审后补提交并推送`
- 下一步：评审后决定是否提交推送。

### ★ medical_review_platform
- 改了什么：继续推进 `repository.py` + 前端的数据库先行/XLSX 后物化，并补测试；同时新增 `datasource/` 和 `scripts/check_review_ids.ps1`。
- 目的：支撑审核数据落库和 review id 批量核对流程。
- 当前建议：`评审后补提交并推送`
- 下一步：评审后决定是否提交推送。

### ★ project-management
- 改了什么：更新仓库巡检、同步建议、待确认提交队列和前端展示逻辑，并回写跟踪数据。
- 目的：把仓库维护从“发现差异”推进到“待确认提交”的闭环流程。
- 当前建议：`评审后补提交并推送`
- 下一步：评审后决定是否提交推送。

### ★ green-lcd-5key
- 改了什么：本次主要改动样本：无。
- 目的：当前目的待人工确认。
- 当前建议：`建议直接推送`
- 下一步：评审后决定是否提交推送。

### ★ laser_factory_platform
- 改了什么：新增设备追溯 API、`trace_repo` 存储、Tab5 追溯页和自检推荐高亮，连带更新 `data/factory.db`。
- 目的：给工厂平台补设备追溯入口和自检定位能力。
- 当前建议：`建议直接推送`
- 下一步：评审后决定是否提交推送。


## 先 Pull

- 无
